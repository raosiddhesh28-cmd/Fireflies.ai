# The API loads .env, then .env.development, then .env.local (later files win).
ANTHROPIC_API_KEY=
# Required for identity-linked console keys (header anthropic-workspace-id).
# Copy wrkspc_… from https://platform.claude.com/settings/workspaces
ANTHROPIC_WORKSPACE_ID=
